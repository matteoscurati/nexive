import $ from 'jQuery';

require('jquery-ui');
require('jquery-ui-touch-punch');
require('modernizr');

$(document).ready(() => {
  const text = "Testo di prova"
  console.log(text, ": funziona");
});
